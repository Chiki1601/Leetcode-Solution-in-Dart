class Solution {
  int removeZeros(int n) {
    int res = 0;
    int mult = 1;
    while (n > 0) {
      int rem = n % 10;
      if (rem != 0) {
        res += rem * mult;
        mult *= 10;
      }
      n ~/= 10;
    }
    return res;
  }
}
